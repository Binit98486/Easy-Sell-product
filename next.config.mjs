/** @type {import('next').NextConfig} */
const nextConfig = {
 images: {
  remotePatterns: [

    {
      protocol: 'https',
      hostname: 'daccbrpstbxihluimsaq.supabase.co',

    },
  ],
},
};

export default nextConfig;
